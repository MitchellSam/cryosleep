/**
 * Rules constants. These live in content, not in the engine or the UI, so that
 * neither hardcodes a rule — see CLAUDE.md.
 */

/** Actions in one player's round — RULES.md §3.1. */
export const ACTIONS_PER_ROUND = 2;

/** Hand size every player draws up to at the start of a Player Phase — RULES.md §3.1. */
export const HAND_SIZE = 5;

export const MIN_PLAYERS = 2;
export const MAX_PLAYERS = 5;
